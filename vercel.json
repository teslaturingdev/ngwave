{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf dist && npm run build:${NGWAVE_APP:-docs} && mkdir -p dist/_deploy && cp -r dist/*/browser/. dist/_deploy/",
  "outputDirectory": "dist/_deploy",
  "functions": { "api/migrate-ai.ts": { "maxDuration": 60 } },
  "rewrites": [{ "source": "/((?!api/|@|.*\\.).*)", "destination": "/index.html" }]
}
